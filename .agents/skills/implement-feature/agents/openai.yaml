interface:
  display_name: "Implement feature"
  short_description: "Build from spec/handoff"
  default_prompt: "Use $implement-feature to implement the current handoff/spec with tests, then hand off for Cursor review."
policy:
  allow_implicit_invocation: true
