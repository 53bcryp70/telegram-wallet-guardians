interface:
  display_name: "Seed-share audit"
  short_description: "Audit scope and secret handling"
  default_prompt: "Use $seed-share-audit to review recent changes against AGENTS.md forbidden features and secret handling."
policy:
  allow_implicit_invocation: true
