interface:
  display_name: "Run tests"
  short_description: "Run and fix the test suite"
  default_prompt: "Use $run-tests to run the project tests and fix failures from my recent changes."
policy:
  allow_implicit_invocation: true
