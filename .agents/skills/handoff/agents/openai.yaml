interface:
  display_name: "Handoff"
  short_description: "Pass work to the other agent"
  default_prompt: "Use $handoff to write a clear handoff for Cursor with acceptance criteria."
policy:
  allow_implicit_invocation: true
